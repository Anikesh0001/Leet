// Supabase client removed in local demo build.
// This file remains for compatibility but exports a no-op placeholder to
// avoid compilation errors from leftover imports.

export const supabase = {
  // developers: replace with localdb usage
};

export type Database = any;
